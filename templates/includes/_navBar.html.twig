
<nav class="navbar navbar-expand-lg navbar-light white-color" role="navigation">
    <a class="navbar-brand" href="{{ path('home') }}">Loue ton Wilder
    </a>
    <div class="d-flex">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
                aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        {% if  is_granted('ROLE_USER') %}
            <a href="{{ path('cart_index') }}" class="icon-profile-mobile">
                <i class="fas fa-shopping-basket fa-2x ml-4"></i>{{ cartWithData |length }}
            </a>
            <div class="dropdown show icon-profile-mobile ml-4">
                <div role="button" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true"
                     aria-expanded="false">
                    <i class="fas fa-user-circle fa-2x"></i>
                </div>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                    <a class="dropdown-item" href="{{ path('user_account') }}">
                        <i class="far fa-address-card"></i>
                        Compte
                    </a>
                    <a class="dropdown-item" href="{{ path('app_logout') }}">
                        <i class="fas fa-sign-out-alt"></i>
                        Déconnexion
                    </a>
                </div>
            </div>
        {% endif %}
    </div>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav d-flex justify-content-end w-100 ul-navbar">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item mr-4 ml-4">
                    <a class="nav-link" href="{{ path('wilder_index') }}" target="_blank">Administrateur</a>
                </li>
            {% endif %}
            {% if not is_granted('ROLE_USER') %}
                <li class="nav-item ml-4">
                    <a class="nav-link" href="{{ path('user_register') }}">Inscription</a>
                </li>
                <li class="nav-item ml-4">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
            {% endif %}
            {% if  is_granted('ROLE_USER') %}
                <a href="{{ path('cart_index') }}" class=" icon-profile-desktop">
                    <i class="fas fa-shopping-basket fa-2x"></i>{{ cartWithData |length }}
                </a>
                <li class="dropdown show ml-4 icon-profile-desktop">
                    <div role="button" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true"
                         aria-expanded="false">
                        <i class="fas fa-user-circle fa-2x"></i>
                    </div>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="{{ path('user_account') }}">
                            <i class="far fa-address-card"></i>
                            Compte
                        </a>
                        <a class="dropdown-item" href="{{ path('app_logout') }}">
                            <i class="fas fa-sign-out-alt"></i>
                            Déconnexion
                        </a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>