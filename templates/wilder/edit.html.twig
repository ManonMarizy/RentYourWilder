{% extends 'base.html.twig' %}

{% block title %}Edit Wilder{% endblock %}

{% block body %}
    <div class="mr-2 ml-2 mt-4">
        <h1 class="text-center">Modifier la fiche de "{{ wilder.name }}"</h1>
        <a href="{{ path('wilder_index') }}">Retour à la liste</a>
        <div class="mt-4">
            {{ include('wilder/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
        <div class="mt-4">
            {{ include('wilder/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
