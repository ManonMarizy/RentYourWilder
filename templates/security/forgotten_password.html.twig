
{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center mt-4 mb-4">Mot de passe oublié</h1>

    <div class="d-flex justify-content-center">
        {{ form_start(form) }}
        {{ form_row(form.email) }}
        <div class="d-flex justify-content-center">
            <button class="btn btn-success mt-3" type="submit">Envoyer</button>
        </div>
        {{ form_end(form) }}
    </div>
{% endblock %}